{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "postinstall": "npm run bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
